interface:
  display_name: "Open Browser Use(OBU)"
  short_description: "Route every browser action through Open Browser Use"
  default_prompt: "Use $open-browser-use to open this URL in my browser and keep the page available."
